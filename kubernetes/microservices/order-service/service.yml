apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: microservices-app
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
